import type { AppConfig } from '~/shims';

export default defineAppConfig({
    version: '',
    login: {
        providers: [],
    },
    readOnly: true,
    updateAvailable: null,
} as AppConfig);
