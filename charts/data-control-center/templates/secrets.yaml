apiVersion: v1
kind: Secret
metadata:
  name: {{ include "data-control-center.fullname" . }}
  labels:
    {{- include "data-control-center.labels" . | nindent 4 }}
data:
  CEPH_DASHBOARD_USERNAME: {{ .Values.config.dashboard.username | default (randAlphaNum 20) | b64enc }}
  CEPH_DASHBOARD_PASSWORD: {{ .Values.config.dashboard.password | default (randAlphaNum 40) | b64enc }}
{{- range $k, $v := .Values.additionalEnv }}
  {{ $k }}: {{ $v | b64enc }}
{{- end }}
