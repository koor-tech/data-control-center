syntax = "proto3";

package services.cluster;
option go_package = "github.com/koor-tech/data-control-center/gen/go/api/services/cluster;cluster";

message StreamRequest {}

message StreamResponse {
    oneof message {
        bool todo = 1;
    }
}

service Stats {
  rpc Stream(StreamRequest) returns (stream StreamResponse);
}
